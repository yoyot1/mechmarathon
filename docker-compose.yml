services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mechmarathon
      POSTGRES_PASSWORD: mechmarathon
      POSTGRES_DB: mechmarathon
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
