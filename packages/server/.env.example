PORT=3000
CLIENT_URL=http://localhost:5173
# Use container name when running via docker-outside-of-docker (devcontainer default)
# Use localhost if running PostgreSQL directly on the host
DATABASE_URL=postgresql://mechmarathon:mechmarathon@mechmarathon-postgres-1:5432/mechmarathon
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
